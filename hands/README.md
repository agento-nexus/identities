# Ghost directory — hand manifests are dynamically generated at runtime by platform/src/lib/executive/identity/manifest-generator.ts from (persona × team × decision context × tool bindings). The static prototype files that lived here were never read by the platform. They were removed when the v2 persona model shipped. This README.md stays as a marker so the directory isn't re-created accidentally.
